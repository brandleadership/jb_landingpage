design.name = {{ properties.template.name }}
design.author = {{ properties.template.author }}
{# 
html-editor-config.full.features={{ properties.htmleditor.features }}
html-editor-config.full.text-colors={{ properties.htmleditor.textcolors }}
html-editor-config.full.background-colors={{ properties.htmleditor.backgroundcolors }}
html-editor-config.full.formats={{ properties.htmleditor.formats }}
html-editor-config.full.font-sizes={{ properties.htmleditor.fontsizes }}
html-editor-config.full.font-size-unit={{ properties.htmleditor.fontsizeunit }}
html-editor-config.full.font-size-default={{ properties.htmleditor.fontsizedefault }}
html-editor-config.full.line-heights={{ properties.htmleditor.lineheights }}
html-editor-config.full.enter={{ properties.htmleditor.enter }}  #}
{# do not add whitespace #}
{% for name, style in properties.template.styles %}
style.{{ name }}.label = {{ style.label }}
{% for class, label in style.classes %}
style.{{ name }}.class.{{ class }}.label = {{ label }}
{% endfor %}
{% endfor %}

{# do not add whitespace #}
{% for group, name in properties.template.groups %}
group.{{ group }}.label = {{ name }}
{% endfor %}

{# do not add whitespace #}
{% for group, elements in properties.template.elements %}
{% for element in elements %}
{% include '../../components/' ~ group ~ '/' ~ element ~ '/design.properties' with properties %}

{# do not add whitespace #}
{% endfor %}
{% endfor %}
