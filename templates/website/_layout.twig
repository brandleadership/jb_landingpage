<!DOCTYPE html>
<html lang="en"
  xmlns="http://www.w3.org/2000/svg"
  xmlns:xlink="http://www.w3.org/1999/xlink">
  <head>
    <meta name="robots" content="noindex" />
    <meta http-equiv="X-UA-Compatible" content="chrome=1" />
    <meta charset="utf-8" />
    <meta name="viewport"
      content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />

    <!-- PLYR library has a CSS and js, both enqueued on this template and added manually into the scr folder -->
    {# <link rel="stylesheet" href="./css/plyr.scss" /> #}
    <link rel="apple-touch-icon"
      sizes="57x57"
      href="{{ bsi_cx_asset('./img/Icons/JB_57.png') }}" />
    <link rel="apple-touch-icon"
      sizes="72x72"
      href="{{ bsi_cx_asset('./img/Icons/JB_72.png') }}" />
    <link rel="apple-touch-icon"
      sizes="76x76"
      href="{{ bsi_cx_asset('./img/Icons/JB_76.png') }}" />
    <link rel="apple-touch-icon"
      sizes="114x114"
      href="{{ bsi_cx_asset('./img/Icons/JB_114.png') }}" />
    <link rel="apple-touch-icon"
      sizes="120x120"
      href="{{ bsi_cx_asset('./img/Icons/JB_120.png') }}" />
    <link rel="apple-touch-icon"
      sizes="144x144"
      href="{{ bsi_cx_asset('./img/Icons/JB_144.png') }}" />
    <link rel="apple-touch-icon"
      sizes="152x152"
      href="{{ bsi_cx_asset('./img/Icons/JB_152.png') }}" />
    <link rel="apple-touch-icon"
      sizes="180x180"
      href="{{ bsi_cx_asset('./img/Icons/JB_180.png') }}" />
    <link rel="icon"
      type="image/png"
      sizes="192x192"
      href="{{ bsi_cx_asset('./img/Icons/JB_192.png') }}" />
    <link rel="icon"
      type="image/png"
      sizes="32x32"
      href="{{ bsi_cx_asset('./img/Icons/JB_32.png') }}" />
    <link rel="icon"
      type="image/png"
      sizes="96x96"
      href="{{ bsi_cx_asset('./img/Icons/JB_96.png') }}" />
    <link rel="icon"
      type="image/png"
      sizes="16x16"
      href="{{ bsi_cx_asset('./img/Icons/JB_16.png') }}" />

    <meta property="og:description"
      content="{header_lead-text}" />
 <title>{% verbatim %}{{ navigation.activePage.pageTitle }}{% endverbatim %}</title>
   <link rel="stylesheet" href="{{ bsi_cx_css_href() }}" />
  </head>
  <body>
   <main class="main" id="main">

  <section class="page-template template-article-page">

{% block header %}

{% endblock %}

{% block content %}
{% endblock %}
       </section>

        </main>

    {# <script src="./js/plyr.js"></script>
    <script data-bsi-remove-if="draft" src="./js/components.js"></script>
    <script data-bsi-remove-if="draft" src="./js/custom.js"></script>
    <script data-bsi-remove-if="draft"> document.getElementById("copyright-year").innerHTML = new Date().getFullYear()</script> #}
     <script src="{{ bsi_cx_js_module_runtime_href() }}"
            defer="defer"></script>
        <script src="{{ bsi_cx_js_module_href('main') }}"
            defer="defer"></script>
              <script src="{{ bsi_cx_js_module_href('root') }}"
            defer="defer"></script>
        {{ bsi_cx_js_module_missing_chunks_import() }}
  </body>
</html>